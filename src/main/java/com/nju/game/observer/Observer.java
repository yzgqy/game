package com.nju.game.observer;

import com.nju.game.hero.HeroAttribute;

public interface Observer {
//    public void update(HeroAttribute heroAttribute);
public void update(HeroAttribute heroAttribute);
}
